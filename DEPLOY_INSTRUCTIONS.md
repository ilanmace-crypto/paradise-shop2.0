# 🚀 Deploy Instructions - Paradise Shop API

## 📋 Что нужно сделать перед деплоем

### 1. Создать Supabase проект
1. Зайди на [supabase.com](https://supabase.com)
2. Войди через GitHub
3. Создай новый проект: `paradise-shop-api`
4. Дождись завершения настройки (1-2 минуты)

### 2. Настроить базу данных
1. В проекте Supabase → SQL Editor
2. Скопируй весь код из `database_schema.sql`
3. Вставь и нажми "Run"
4. Проверь, что таблицы создались в Table Editor

### 3. Получить DATABASE_URL
1. Settings → Database → Connection string
2. Скопируй строку подключения
3. Замени `[YOUR-PASSWORD]` на пароль от БД

## 🌐 Деплой на Railway (рекомендуется)

### Шаг 1: Fork репозитория
```bash
# Fork этого репозитория на GitHub
# Затем клонируй если нужно локально
git clone https://github.com/TWOTVOIUSERNAME/paradise-shop-api.git
cd paradise-shop-api
```

### Шаг 2: Деплой на Railway
1. Зайди на [railway.app](https://railway.app)
2. Войди через GitHub
3. Нажми "New Project" → "Deploy from GitHub repo"
4. Выбери `paradise-shop-api`
5. Нажми "Deploy Now"

### Шаг 3: Настройка переменных
1. В проекте Railway → Variables
2. Добавь переменные:
   ```
   DATABASE_URL=postgresql://postgres:[PASSWORD]@[PROJECT].supabase.co:5432/postgres
   NODE_ENV=production
   JWT_SECRET=your-super-secret-jwt-key-change-this
   ```

### Шаг 4: Редеплой
1. Нажми "Redeploy" в Railway
2. Дождись завершения деплоя
3. Проверь работу API по URL Railway

## 🌐 Деплой на Vercel

### Шаг 1: Импорт репозитория
1. Зайди на [vercel.com](https://vercel.com)
2. New Project → Import GitHub Repository
3. Выбери `paradise-shop-api`

### Шаг 2: Настройка
1. Environment Variables:
   ```
   DATABASE_URL=postgresql://postgres:[PASSWORD]@[PROJECT].supabase.co:5432/postgres
   NODE_ENV=production
   JWT_SECRET=your-super-secret-jwt-key-change-this
   ```

### Шаг 3: Деплой
1. Нажми "Deploy"
2. Дождись завершения

## ✅ Проверка работы

### Health Check
```bash
curl https://your-app-url.railway.app/health
```

### Products API
```bash
curl https://your-app-url.railway.app/api/products
```

## 🔧 Локальная разработка

### Установка
```bash
npm install
cd server && npm install
```

### Запуск
```bash
# Создай .env файл с DATABASE_URL
cp .env.example .env
# Отредактируй .env с твоими данными

npm run dev
```

## 🚨 Возможные проблемы

### 1. Ошибка подключения к БД
- Проверь `DATABASE_URL` в переменных окружения
- Убедись, что пароль правильный
- Проверь, что Supabase проект запущен

### 2. Ошибка деплоя
- Проверь все переменные окружения
- Убедись, что `package.json` правильный
- Проверь логи деплоя

### 3. API не работает
- Проверь health endpoint
- Посмотри логи в Railway/Vercel
- Проверь подключение к Supabase

## 📞 Поддержка

Если что-то не работает:
1. Проверь логи деплоя
2. Проверь переменные окружения
3. Убедись, что БД создана правильно
4. Создай Issue в GitHub репозитории

---

**Готово! Твой API теперь работает на Supabase бесплатно! 🎉**
